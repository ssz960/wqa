# Proposed GPT batches

GPT outputs land here as `REVIEW_ONLY`. They must not be imported or uploaded to the
server until Codex validates schema, scope, fields, operators, duplicates, and activity gates.

