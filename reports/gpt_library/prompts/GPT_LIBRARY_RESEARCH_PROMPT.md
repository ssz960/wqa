# GPT research instruction

Use the library index and catalog to select only material relevant to the requested
region, delay, universe, activity, and dataset/theme. Distinguish official/API evidence
from forum experience and inference. Resolve every field and operator against supplied
indexes before using it.

When a completed research pack is supplied, summarize winners, failures, correlations,
turnover, robustness checks, and missing evidence before generating a new batch. Produce
the requested strict CSV schema with source resource IDs, template family, scope, and
`REVIEW_ONLY` status. Do not claim a test passed unless the pack contains the official
result. Power Pool candidates must be USA/D1/TOP1000 and retain
`HIGH_TURNOVER_RETURNS_RATIO` as a required test gate until proven PASS.

